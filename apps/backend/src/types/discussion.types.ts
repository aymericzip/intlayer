import { RenameId } from '@utils/mongoDB/types';
import { Document, Model, Types } from 'mongoose';

export interface Message {
  role: 'user' | 'assistant' | 'system';
  content: string;
  timestamp: Date;
}

export interface Discussion extends Document {
  id: Types.ObjectId; // DB id
  discutionId: string; // ID generated by the client for each discussion before it gets saved in the DB
  messages: Message[];
  userId: Types.ObjectId;
  projectId: Types.ObjectId;
  organizationId: Types.ObjectId;
  title?: string;
  isArchived: boolean;
  createdAt: Date;
  updatedAt: Date;
}

export type DiscussionSchema = RenameId<Discussion>;
export type DiscussionModelType = Model<Discussion>;
export type DiscussionDocument = Document<unknown, {}, Discussion> & Discussion;
