For docs

- Keep everything concise as possible
- Focus on final user point of view. Avoid mention of internal subtleties
- Don't use '—' char

If not explicitly mentioned, never use AI generation for translations using the scripts `translate` or `apply` + `MODE = 'apply'`. Those will consume extra token no included in the plan.
