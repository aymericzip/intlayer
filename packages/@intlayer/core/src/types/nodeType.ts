export enum NodeType {
  Translation = 'translation',
  Enumeration = 'enumeration',
  Condition = 'condition',
  Object = 'object',
  Array = 'array',
  Text = 'text',
  Nested = 'nested',
  ReactNode = 'reactNode',
  Markdown = 'markdown',
}
